import React from 'react';

const ItemListContainer = ({greeting}) => {
    return(
     
        <section>

            <p className='texto_principal'>{greeting}</p>

        </section>

    
    );

}

export default ItemListContainer;